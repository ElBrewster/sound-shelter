import { writable } from "svelte/store";

export const beddingCount = writable(200);
export const cashCount = writable(200);
export const clothingCount = writable(200);
export const foodCount = writable(200);
export const medicalCount = writable(200);
export const otherCount = writable(200);
export const petSuppliesCount = writable(200);
export const toiletriesCount = writable(200);