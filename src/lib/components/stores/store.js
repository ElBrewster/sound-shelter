import { writable } from "svelte/store";

export const beddingCount = writable(0);
export const cashCount = writable(0);
export const clothingCount = writable(0);
export const foodCount = writable(0);
export const medicalCount = writable(0);
export const petSuppliesCount = writable(0);
export const toiletriesCount = writable(0);